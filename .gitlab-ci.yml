cache:
  paths:
  - node_modules/
before_script:
  - ./script/prebuild.sh
lint:
  script:
  - npm run tslint
arlas-web-contributors-doc:
  script:
  - npm run build-release;mv dist/docs .
  artifacts:
    paths:
    - docs
    name: "arlas-web-contributors-doc"